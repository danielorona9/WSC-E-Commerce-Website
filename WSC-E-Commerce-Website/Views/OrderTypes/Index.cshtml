@model IEnumerable<WSC_E_Commerce_Website.Models.OrderType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.OrderTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.OrderTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OrderTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.OrderTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OrderTypeID })
        </td>
    </tr>
}

</table>
